$("#addressModal").remove();
$(".modal-backdrop").remove();
$("body").prepend("<%= escape_javascript(render partial: 'new', locals: { patient_id: 1, origin: 'patient' }) %>")
$(".alert").remove();
$("#addressModal").modal("show");
$("input[type=\"text\"]").setMask();