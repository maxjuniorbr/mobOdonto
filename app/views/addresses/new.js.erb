$("#address-modal").remove();
$(".modal-backdrop").remove();
$("body").prepend("<%= escape_javascript(render 'new') %>")
$(".alert").remove();
$("#address-modal").modal("show");
$("input[type=\"text\"]").setMask();